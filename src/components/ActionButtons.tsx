import { useToast } from "@/hooks/use-toast";

type ActionButtonsProps = {
  onSend?: (message: string) => void;
};

const ActionButtons = ({ onSend }: ActionButtonsProps) => {
  return null; // Remove all buttons
};

export default ActionButtons;